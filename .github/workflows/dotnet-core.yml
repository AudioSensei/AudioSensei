name: .NET Core CI

on: [push, pull_request]

jobs:
  build:

    name: .Net Core ${{ matrix.framework }} on ${{ matrix.os }} for ${{ matrix.target }}-${{ matrix.arch }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [windows-latest, ubuntu-latest]
        framework: ['3.1']
        arch: ['x64']
        include: 
        - os: windows-latest
          target: win
        - os: ubuntu-latest
          target: linux
        - arch: 'x86'
          os: windows-latest
    timeout-minutes: 30

    steps:
    - uses: actions/checkout@v2.2.0
      
    - name: Setup .NET Core
      uses: actions/setup-dotnet@v1.5.0
      with:
        dotnet-version: ${{ matrix.framework }}
      
    - name: Install dependencies
      run: dotnet restore -r ${{ matrix.target }}-${{ matrix.arch }}
      
    - name: Build ${{ matrix.target }}
      run: dotnet publish -r ${{ matrix.target }}-${{ matrix.arch }} -c release -p:PublishReadyToRunShowWarnings=true
      
    - name: Test
      run: dotnet test --runtime ${{ matrix.target }}-${{ matrix.arch }}
      
    - name: Upload ${{ matrix.target }}
      uses: actions/upload-artifact@v2.0.1
      with:
        name: AudioSensei-${{ matrix.target }}
        path: ${{ github.workspace }}/AudioSensei/bin/Release/netcoreapp${{ matrix.framework }}/${{ matrix.target }}-${{ matrix.arch }}/publish
